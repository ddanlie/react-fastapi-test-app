


    server {
    listen 80;
    server_name yourdomain.com;

    # Serve React static files (JS, CSS, images)
    #location /app/assets/ {
    #    root /path/to/your/project/frontend/dist;
    #}

    # Serve index.html for any /app/* route
    location /app/ {
        # alias has to be full path
        alias /home/ /react-crm-hw/frontend;
        index index.html;
        try_files $uri @react_fallback;
    }

    location @react_fallback {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Proxy API calls to FastAPI
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
